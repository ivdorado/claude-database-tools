# Dependencies
node_modules/

# Build output
dist/

# Environment files (contain credentials). Live convention now stores these
# in a per-user config directory outside the repo (see README#Configuration);
# these entries are just a backstop for anyone still using the pre-plugin
# project-root layout.
.env
.env.local
.env.*.local

# Multi-client SQL connection config (contains credentials) — same backstop.
clients.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Temporary files
*.tmp
*.temp
